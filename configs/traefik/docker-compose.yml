services:
  traefik:
    image: traefik:latest
    container_name: traefik
    restart: unless-stopped
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.file.directory=/etc/traefik/dynamic"
      - "--providers.file.watch=true"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
    ports:
      - "80:80"
      - "443:443"
      - "8888:8080"  # Traefik dashboard on :8888
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./dynamic:/etc/traefik/dynamic:ro
    environment:
      TZ: Australia/Sydney
    labels:
      - traefik.enable=true
      - traefik.http.routers.traefik-http.rule=Host(`traefik.homelab.internal`)
      - traefik.http.routers.traefik-http.entrypoints=web
      - traefik.http.routers.traefik-http.middlewares=traefik-https-redirect
      - traefik.http.middlewares.traefik-https-redirect.redirectscheme.scheme=https
      - traefik.http.routers.traefik-http.service=traefik
      - traefik.http.routers.traefik-https.rule=Host(`traefik.homelab.internal`)
      - traefik.http.routers.traefik-https.entrypoints=websecure
      - traefik.http.routers.traefik-https.tls=true
      - traefik.http.routers.traefik-https.middlewares=authentik@file
      - traefik.http.routers.traefik-https.service=traefik
      - traefik.http.services.traefik.loadbalancer.server.port=8080
    networks:
      - traefik
      - arrnetwork

networks:
  traefik:
    name: traefik
    driver: bridge
  arrnetwork:
    name: arrnetwork
    external: true
